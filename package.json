{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap --force-local",
    "build": "lerna exec --stream --scope=dashboard -- yarn build",
    "start": "lerna exec --stream --scope=dashboard -- yarn start"
  },
  "dependencies": {
    "dotenv-cli": "^2.0.1",
    "lerna": "^3.13.1",
    "webpack-cli": "^3.3.8"
  }
}
